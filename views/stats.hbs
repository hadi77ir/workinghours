<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daily Statistics - Hours Tracker</title>
    <link rel="stylesheet" href="/static/bulma.min.css">
    <style>
        .hero {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        .stats-table {
            background: white;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .table-container {
            border-radius: 8px;
            overflow: hidden;
        }
        .total-time {
            font-size: 1.5rem;
            font-weight: bold;
            color: #667eea;
        }
    </style>
</head>
<body>
    <section class="hero is-primary is-medium">
        <div class="hero-body">
            <div class="container has-text-centered">
                <h1 class="title is-1">
                    📊 Daily Statistics
                </h1>
                <p class="subtitle is-4">
                    View your work hours by day
                </p>
            </div>
        </div>
    </section>

    <section class="section">
        <div class="container">
            <div class="columns is-centered">
                <div class="column is-10">
                    <div class="box stats-table">
                        <div class="level mb-4">
                            <div class="level-left">
                                <div class="level-item">
                                    <h2 class="title is-4">Work Hours Summary</h2>
                                </div>
                            </div>
                            <div class="level-right">
                                <div class="level-item">
                                    <a href="/" class="button is-link is-light">
                                        <span class="icon">
                                            <span>🏠</span>
                                        </span>
                                        <span>Back to Tracker</span>
                                    </a>
                                </div>
                            </div>
                        </div>

                        <form method="get" class="box has-background-light mb-5">
                            <div class="field">
                                <label class="label">Select Working Group</label>
                                <div class="control">
                                    <div class="select is-medium">
                                        <select name="group_id" onchange="this.form.submit()">
                                            {{#each GroupOptions}}
                                                <option value="{{ID}}" {{#if Selected}}selected{{/if}}>{{Name}}</option>
                                            {{/each}}
                                        </select>
                                    </div>
                                </div>
                            </div>
                        </form>

                        <div class="columns is-multiline">
                            <div class="column is-one-third">
                                <div class="notification is-primary is-light has-text-centered">
                                    <p class="heading">Total Today ({{SelectedGroupName}})</p>
                                    <p class="title is-4">{{SelectedGroupTodayFormatted}}</p>
                                </div>
                            </div>
                            <div class="column is-one-third">
                                <div class="notification is-primary is-light has-text-centered">
                                    <p class="heading">Total ({{SelectedGroupName}})</p>
                                    <p class="title is-4">{{SelectedGroupTotalFormatted}}</p>
                                </div>
                            </div>
                            <div class="column is-one-third">
                                <div class="notification is-link is-light has-text-centered">
                                    <p class="heading">Total (All Working Groups)</p>
                                    <p class="title is-4">{{AllGroupsTotalFormatted}}</p>
                                </div>
                            </div>
                        </div>

                        <h3 class="title is-5 mt-5">Daily Summary ({{SelectedGroupName}})</h3>

                        {{#if DailySummaries}}
                        <div class="table-container">
                            <table class="table is-fullwidth is-striped is-hoverable">
                                <thead>
                                    <tr>
                                        <th>Date</th>
                                        <th class="has-text-centered">Rounds</th>
                                        <th class="has-text-right">Total Time</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {{#each DailySummaries}}
                                    <tr>
                                        <td>
                                            <strong>{{DateDisplay}}</strong>
                                            <br>
                                            <small class="has-text-grey">{{Date}}</small>
                                        </td>
                                        <td class="has-text-centered">
                                            <span class="tag is-info is-light">{{RoundCount}} round(s)</span>
                                        </td>
                                        <td class="has-text-right">
                                            <span class="total-time">{{TotalFormatted}}</span>
                                        </td>
                                    </tr>
                                    {{/each}}
                                </tbody>
                            </table>
                        </div>
                        {{else}}
                        <div class="notification is-info is-light">
                            <p class="has-text-centered">
                                <span class="icon is-large">
                                    <span style="font-size: 3rem;">📭</span>
                                </span>
                            </p>
                            <p class="has-text-centered title is-5">No completed rounds yet for this working group</p>
                            <p class="has-text-centered">Start tracking your work to see statistics here!</p>
                        </div>
                        {{/if}}

                        <h3 class="title is-5 mt-6">Totals by Working Group</h3>
                        <div class="table-container">
                            <table class="table is-fullwidth is-striped">
                                <thead>
                                    <tr>
                                        <th>Working Group</th>
                                        <th class="has-text-right">Total Time</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {{#each GroupTotals}}
                                    <tr>
                                        <td>{{GroupName}}</td>
                                        <td class="has-text-right">{{TotalFormatted}}</td>
                                    </tr>
                                    {{/each}}
                                </tbody>
                            </table>
                        </div>

                        <div class="has-text-centered mt-5">
                            <a href="/export/csv?group_id={{SelectedGroupID}}" class="button is-success is-light">
                                <span class="icon">
                                    <span>📥</span>
                                </span>
                                <span>Export to CSV</span>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer class="footer">
        <div class="content has-text-centered">
            <p>
                <strong>Hours Tracker</strong> - Built with Fiber, GORM, Bulma & HTMX
            </p>
        </div>
    </footer>
</body>
</html>

